import { DynamicPageDirective } from './dynamic-page.directive';

describe('DynamicPageDirective', () => {
  it('should create an instance', () => {
    const directive = new DynamicPageDirective();
    expect(directive).toBeTruthy();
  });
});
